# External development computer only. Do not place this file under application/.
export ROBOT_PROJECT_ROOT="$HOME/Robot_Arm_Project"

# Wired network: change to the actual IPs assigned to the vehicle and this PC.
export VEHICLE_IP="192.168.2.10"
export LOCAL_VISION_IP="192.168.2.20"
export MASTER_NODE_HTTP_PORT="1819"

# CR5 ROS2 driver reads these exact environment variable names.
export IP_address="192.168.5.1"
export DOBOT_TYPE="cr5"

# Keep 127.0.0.1 for local commissioning. For a wired vehicle connection,
# set this to this computer's wired IP and set VEHICLE_IP above to the car IP.
export VISION_RPC_HOST="127.0.0.1"
# Generate a long random value; it is mandatory for non-loopback RPC.
export VISION_RPC_AUTH_TOKEN=""

# Legacy NX compatibility. The vehicle master_node is hard-coded to connect to
# 192.168.2.103:8888. Add that address to this PC's wired NIC before enabling
# the gateway, then route each real map/pose ID in nx_routes.json.
export NX_COMPAT_ENABLED="false"
export NX_COMPAT_HOST="0.0.0.0"
export NX_COMPAT_PORT="8888"
export NX_COMPAT_ALLOWED_CLIENTS="${VEHICLE_IP}/32"
export NX_ROUTE_FILE="${ROBOT_PROJECT_ROOT}/src/vision_arm_executor/config/nx_routes.json"
export NX_VISION_RPC_HOST="${VISION_RPC_HOST}"
export NX_VISION_RPC_PORT="17881"

# Safety defaults. The visual executor still starts disabled even if this says true.
export VISION_DRY_RUN="true"
export ROS_DOMAIN_ID="0"
