# 兼容 TCP_ESTUN 代码开发流程

本项目是不依赖 ROS2 运行时的 ESTUN + D455 视觉示教与执行后端。AprilTag 只使用开源检测算法库；Tag 和 ICP 的示教、定位、保存及执行链路均在 `TCP_asdun_arm` 中重新编写，未复用原 ROS2 Tag/ICP 业务代码。

## 项目使用文件

```text
Robot_Arm_Project/
├── TCP_asdun_arm/
│   ├── apriltag/                              AprilTag 开源 C 检测算法库
│   ├── config/tagtask.json                    Tag 尺寸、记录目录及标定文件入口
│   └── CodroidApi/
│       ├── CMakeLists.txt                     RobotControl 编译入口与依赖
│       ├── compileDemo.sh                     RobotControl 编译脚本
│       ├── runDemo.sh                         RobotControl 启动脚本
│       ├── include/CodroidApi.h               ESTUN Codroid 控制接口封装
│       └── src/
│           ├── RobotDemo.cpp                  TCP 8888 入口、报文分流和任务调度
│           ├── WebSocketClient.cpp            与 ESTUN 控制柜的 WebSocket 通信
│           ├── D455Camera.cpp                 D455 按需启停，获取 RGB 图像和新点云
│           ├── icpwallfilter.cpp               ICP 墙面检测与剔除
│           ├── icptask.cpp                     ICP Ref 模板和 Work 相对位姿记录
│           ├── icpexecutetask.cpp              ICP 初值搜索、回正和 Work 执行
│           ├── tagtask.cpp                     Tag 检测定位及 Ref/Work 记录
│           └── tagexecutetask.cpp              Tag 初值搜索、定位和 Work 执行
├── scripts/
│   ├── handeye_chessboard.py                  棋盘格手眼标定，生成 T_flange_camera
│   └── tcp_calibration/
│       ├── tcp_pivot_calibrator.py            TCP 固定点标定与结果保存
│       └── tcp_analyze_json.py                TCP 标定样本和结果离线分析
└── data/vision_arm_estun/calibration/
    ├── handeye/active.json                     运行时当前手眼标定结果
    └── tcp/active.json                         运行时当前 TCP 标定结果
```

## 1. TCP 8888 解析入口

`CodroidApi/src/RobotDemo.cpp`

```text
TCP JSON
  → 合法性校验
  → 按 type 分流：type1 示教 / type2 查询 / type3 删除
  → point_type：无=普通点，0=AprilTag，1=ICP
  → "type"= mechanical_arm_command 并携带 command的时候：command = 1 记录 Ref，>2 记录 Work
```

严格边界：`point_type` 只允许出现在开始示教的 `type1`；记录报文不得再次携带 `point_type`。

配置：`config/tagtask.json`

- `tag_size_mm`：Tag 黑色外框边长，单位 mm。
- `handeye_json`：手眼标定入口，提供 `T_flange_camera`。
- `tcp_calibration_json`：TCP 标定入口，供后续执行时做末端偏移补偿；Type1 录制不参与变换计算。



## 2. Type1 示教录制



### 2.1 普通点（无 `point_type`）

- 开始：
```json
{"type":"type1","mapid":"M","poseid":"P"}
```
startTeaching()` 开始拖拽并创建 `mapid/poseid`。
- 记录：
```json
{"type":"mechanical_arm_command","mapid":"M","poseid":"P","command":1}`
```
normalRecord()` 读取机械臂 `angle + pose`，保存为 `1.json`。

### 2.2 AprilTag 点（`point_type=0`）

Ref：

```json
{"type":"type1","mapid":"M","poseid":"P","point_type":0}
{"type":"mechanical_arm_command","mapid":"M","poseid":"P","command":1}
```

1. 图像：`D455Camera.startColorOnly()` 预热 → 机械臂连续停稳 2 s → `captureColorOne()` 丢弃旧帧，输出一帧 `D455Frame`（BGR、内参、畸变、时间戳）。
2. 识别：`TagTask::recordReference(mapid, poseid, frame, tag_id, error)` 输入彩色帧和 `tag_id`，按旧 ROS2 角点顺序调用 `solvePnP()`，得到 `T_camera_tag`。
3. 位姿：`normalRecord()` 保存 `angle + pose`；`TagTask` 读取手眼标定，计算 `T_base_tag = T_base_flange * T_flange_camera * T_camera_tag`，写入 `1.json/tag_pose`。

Work：

```json
{"type":"mechanical_arm_command","mapid":"M","poseid":"P","command":2}
```

停稳 2 s → `normalRecord()` 保存当前 `angle + pose` → `TagTask::recordWork()` 写入 `trans = inv(T_base_tag_ref) * T_base_flange_work`。Work 不开相机。

### 2.3 ICP 点（`point_type=1`）

Ref：

```json
{"type":"type1","mapid":"M","poseid":"P","point_type":1}
{"type":"mechanical_arm_command","mapid":"M","poseid":"P","command":1}
```
相机调用
停稳 2 s → `D455Camera.start()` 按需启动彩色+深度 → `captureOne()` 获取 5 帧新点云 → `normalRecord()` 保存 `angle + pose` → `IcpTask::recordReference()` 融合并保存 `1_20mm.ply, 1_10mm.ply, 1_5mm.ply`。

Work：

```json
{"type":"mechanical_arm_command","mapid":"M","poseid":"P","command":2}
```

停稳 2 s → `normalRecord()` 保存当前 `angle + pose` → `IcpTask::recordWork()` 写入 `trans = inv(T_base_flange_ref) * T_base_flange_work`。Work 不开相机。

Type1 已接入 `RobotDemo.cpp` 的正式 TCP 响应；报文字段会校验，视觉记录中途失败会清理本次 command 的不完整文件。

## 3. Type2 查询

```json
{"type":"type2","mapid":"M","poseid":"P"}
```

## 4. Type3 删除

删除整个点位：

```json
{"type":"type3","mapid":"M","poseid":"P"}
```

删除一个 command：

```json
{"type":"type3","mapid":"M","poseid":"P","command":2}
```

## 5. 生产入口与执行边界

`CodroidApi/src/RobotDemo.cpp` 现为唯一生产入口：TCP 8888 解析、示教、AprilTag/ICP/普通点执行和 Codroid 控制均在同一进程内，不依赖 ROS2。

- 机械臂空站点 `command=1..6`：复位、上电、下电、开始拖拽、结束拖拽、清除报警。
- `type2` 结束当前示教时：`ToAuto -> 复位 -> 到点确认 -> 查询`。
- 站点执行失败时：复位,并返回 `status=failed`。
- 新请求正在执行时，后到请求直接返回 `busy`，不排队，避免旧命令在之后突然执行。

点位固定保存在 `TCP_asdun_arm/records/Mapid/Poseid/n.json`，由 `config/tagtask.json` 的`record_root` 统一指定。命名为

夹爪节点
ros2 run step_motor motor_node
夹爪闭合
ros2 topic pub --once /motor_control step_motor/msg/Motor "{id: 1,speed: 200,dir: 1,mode: 2,angle: 30000,state: 0,sub_divide: 32}"
夹爪张开
ros2 topic pub --once /motor_control step_motor/msg/Motor "{id: 1,speed: 200,dir: 0,mode: 2,angle: 30000,state: 0,sub_divide: 32}"
# 编译加运行
ssh nvidia@192.168.2.103
密码192.168.2.103
```bash
cd /home/nvidia/Robot_Arm_Project/TCP_asdun_arm/CodroidApi
./compileDemo.sh
./runDemo.sh
```
